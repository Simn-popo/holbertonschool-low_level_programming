#include <stdio.h>
/**
 * main - print alpha in minuscule
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 97; n < 123; n++)
	{
	putchar(n);
	}
	putchar('\n');
	return (0);
}
