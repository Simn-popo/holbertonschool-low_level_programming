#include <stdio.h>
/**
 * main - print alpha in minuscule
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int c;

	for (c = 80; c < 123; c++)
	{
	putchar(c);
	}
	putchar('\n');
	return (0);
}
